// src/types/global.d.ts
export {};

declare global {
  interface Window {
    /**
     * 旧 Safari 用のプレフィックス付き AudioContext
     * 存在しない環境では undefined
     */
    webkitAudioContext?: typeof AudioContext;
  }
}
